---
# see for details: https://yamllint.readthedocs.io/en/stable/index.html
extends: default
ignore: |
    .git/
    .github/
    helm/
    tests/molecule
rules:
  line-length:
    max: 120
    level: warning
